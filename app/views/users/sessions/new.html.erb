<div class="container-fluid connexion signin connexion-user">
  <div class="connexion-overlay"></div>
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-6 col-sm-offset-3 form-card">
        <span>Me connecter</span>
        <!-- SIGNIN FORM -->
        <div class="subscription-form">
          <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <%= f.error_notification %>
            <%= f.input :email, placeholder: 'Adresse email', required: true, :class => "validate", label: false %>
            <%= f.input :password, placeholder: 'Mot de passe', required: true, :class => "validate", label: false %>
            <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
            <%= f.button :submit, "Connexion", class: "btn btn-connexion", style: "width:100%;" %>
            <% if devise_mapping.recoverable? && controller_name != 'passwords' %>
            <%= link_to t(".forgot_your_password", :default => "Mot de passe oublié ?"), new_password_path(resource_name), class: "new-password" %>
            <% end %>
          <% end %>
        </div>
        <!-- FB & GOOGLE SUBSCRIPTION BUTTONS -->
        <div class="subscription-network">
          <span>ou</span>
          <%= link_to raw("<i class=\"fa fa-facebook\"></i>"), user_omniauth_authorize_path(:facebook), class: "btn-facebook" %>
          <%= link_to raw("<i class=\"fa fa-google\"></i>"), user_omniauth_authorize_path(:google_oauth2), class: "btn-google" %>
        </div>
      </div>
      <div class="col-xs-12 already-subscribed">
        <span class=""> Pas de compte ? <strong><%= link_to t(".connexion", :default => "Je m'inscris"), new_registration_path(resource_name) %></strong></span>
      </div>
    </div>
  </div>
</div>

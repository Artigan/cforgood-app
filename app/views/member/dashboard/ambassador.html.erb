<%= content_for(:title) do %>
  Espace ambassadeur
<% end %>
<%= content_for(:description) do %>
  Votre espace ambassadeur
<% end %>

<div class="container-fluid admin">
  <!-- Sub Menu Sidebar -->
  <%= render "member/dashboard/shared/sidebar" %>
  <!-- Content -->
  <div class="dashboard ambassador <%= current_user.ambassador %>">
    <% if !current_user.ambassador %>
      <!-- NOT AMBASSADOR -->
      <div class="picture-container">
        <% if current_user.picture? %>
          <%= image_tag current_user.picture.url(:avatar), class: "img-circle user" %>
        <% else %>
          <div class="picture-none">
            <%= render 'shared/icons/profile' %>
          </div>
        <% end %>
      </div>
      <h2> Rejoignez la communauté<br>Cforgood</h2>
      <p class="testimonial">« Ils sont écolos, végétarien, trient leurs déchets, aiment les médecines alternatives… OU PAS !
      Ils sont surtout à l'affût, conscients des enjeux et ont envie de changement à leur échelle.
      Grâce à eux, la Révolution Positive est possible. »</p>

      <p class="testi-bis">Rejoignez la communauté des Ambassadeurs et participez à la Révolution Positive en détectant et en impliquant les acteurs qui vont dans le bon sens autour de vous.

      Vous allez vivre de l'intérieur cette révolution et aurez plus que jamais la parole pour faire grandir CforGood.

      On vous promet une aventure folle pleine de rencontres bienveillantes, de bonnes idées, de surprises, de challenges et d'événements enthousiasmants !.</p>
      <!-- <span class="label-info">Demande en cours de modération</span> -->
      <a class="button-link" href="https://cforgood1.typeform.com/to/iZ5QFx" data-mode="1" target="_blank">Devenir Ambassadeur</a>
    <% else %>
      <!-- AMBASSADOR -->
      <div class="ambassasor-container">
        <!-- Header -->
        <div class="header-ambassador">
          <div class="picture-container">
            <% if current_user.picture? %>
              <%= image_tag current_user.picture.url(:avatar), class: "img-circle user" %>
            <% else %>
              <div class="picture-none">
                <%= render 'shared/icons/profile' %>
              </div>
            <% end %>
          </div>
          <div class="user-status">
            <% if current_user.last_name != "" && current_user.last_name != nil %>
              <p><%= current_user.first_name %> <%= current_user.last_name.chars.first + "." %></p>
            <% elsif current_user.name != "" && current_user.name != nil %>
              <p><%= current_user.name.split(" ")[0] %></p>
            <% end %>
            <span class="label-info">Ambassadeur</span>
          </div>
          <button class="button-link form">Déclarer un commerce</button>
        </div>
        <!-- Pitch -->
        <div class="pitch">
          <div class="pitch-text">
          <h3>Le Pitch</h3>
          <ul>
            <li>
              <h4>Pour les membres</h4>
              <p>CforGood est une Carte de Consommation Positive qui permet de bénéficier de réductions chez les commerces responsables autour de soi en échange d’un don libre à une association.</p>
            </li>
             <li>
              <h4>Pour les commerçants</h4>
              <p>CforGood est une plateforme qui connecte en temps réel les commerçants responsables aux citoyens autour d’eux, pour les aider à se faire connaitre et à vendre à temps leurs produits à prix réduits.</p>
            </li>
             <li>
              <h4>Pour les associations</h4>
              <p>CforGood est une plateforme collaborative qui favorise le soutien associatif en offrant aux citoyens un moyen simple et valorisant de vous aider !</p>
            </li>
          </ul>
           <button class="button-more see-more">En voir +</button>
          </div>
          <div class="pitch-video">
            <div class="embed-container video">
              <iframe class="border-video" width="500" height="285" src="https://www.youtube.com/embed/4tYrdTm6lwQ?controls=0&rel=0&showinfo=0" frameborder="0"></iframe>
            </div>
          </div>
        </div>
        <!-- Tools -->
        <div class="tools">
          <h3>Vos outils</h3>
          <ul>
            <li>
              <button class='button-link-square modal-presentation'>
                <%= render 'shared/icons/view_headline' %>
              </button>
              <span>Présentation</span>
            </li>
            <li>
              <button class='button-link-square modal-community'>
                <%= render 'shared/icons/location_city' %>
              </button>
              <span>Lancer la communauté dans ma ville</span>
            </li>
            <li>
             <button class='button-link-square modal-prospection'>
                <%= render 'shared/icons/turned_in_not' %>
              </button>
              <span>Guide de prospection</span>
            </li>
            <li>
              <button class='button-link-square modal-questions'>
                <%= render 'shared/icons/highlight' %>
              </button>
              <span>Foires aux questions</span>
            </li>
            <li>
              <button class='button-link-square modal-ideas'>
                <%= render 'shared/icons/plus_one' %>
              </button>
              <span>Boîte à idées</span>
            </li>
            <li>
              <button class='button-link-square modal-kit'>
                <%= render 'shared/icons/plus' %>
              </button>
              <span>Kit Ambassadeur</span>
            </li>
          </ul>
        </div>
      </div>
    <% end %>
  </div>
</div>

<%= render partial: 'components/modals/modal_template', locals: {id: "see-more", source: "http://bit.ly/2dvcJcm", style:"width:100%; height:600px; border:0;"} %>
<%= render partial: 'components/modals/modal_template', locals: {id: "modal-presentation", source: "http://bit.ly/commerçant_cforgood", style:"width:100%; height:600px; border:0;"} %>
<%= render partial: 'components/modals/modal_template', locals: {id: "modal-community", source: " http://bit.ly/nouvelle_ville", style:"width:100%; height:600px; border:0;"} %>
<%= render partial: 'components/modals/modal_template', locals: {id: "modal-prospection", source: "http://bit.ly/guide_prospection", style:"width:100%; height:600px; border:0;"} %>
<%= render partial: 'components/modals/modal_template', locals: {id: "modal-questions", source: "http://bit.ly/FAQ_cforgood", style:"width:100%; height:600px; border:0;"} %>
<%= render partial: 'components/modals/modal_template', locals: {id: "modal-ideas", source: "http://bit.ly/boite_idée", style:"width:100%; height:600px; border:0;"} %>
<%= render partial: 'components/modals/modal_template', locals: {id: "modal-kit", source: "http://bit.ly/2cW8KBQ", style:"width:100%; height:600px; border:0;"} %>

<%= render(partial: 'member/prospects/form/create', locals: { prospect: Prospect.new }) %>

<% content_for :after_js do %>

  <script>

    $(document).ready(function() {

      $(".button-link-square").click(function() {
        var idModal = $(this).attr('class').split(" ")[1];
        $("#"+idModal).removeClass('hidden');
      });

      $(".button-more").click(function() {
        var idModal = $(this).attr('class').split(" ")[1];
        $("#"+idModal).removeClass('hidden');
      });

      $('.button-close').click(function() {
        var idModal = $(this).attr('class').split(" ")[1];
        $("#"+idModal).addClass('hidden');
      });

      $('.button-link.form').click(function() {
        $(".form-create-prospect").removeClass('hidden');
      });

    });

  </script>

<% end %>


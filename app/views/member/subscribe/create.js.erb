
<% if flash[:success].present? %>

  $(".spinner").addClass('hidden');
  $('.btn-submit').attr("disabled", false);
  $('.button-gradient').attr("disabled", false);
  // $(".overlay-payment").removeClass('hidden');

<% else %>

  $(".spinner").addClass('hidden');
  $('.btn-submit').attr("disabled", false);
  $(".overlay-payment").removeClass('hidden');
  $('.button-gradient').attr("disabled", false);

<% end %>

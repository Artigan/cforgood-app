<% content_for :meta_title, "Rejoignez-nous !" %>
<% content_for :meta_description %>
<% content_for :meta_image %>

<div class="connexion">
  <span>Connexions</span>
  <div class="signin-user">
    <!-- SIGNIN FORM USER -->
    <%= render "devise/sessions/shared/form_signin", resource_name: :user %>
  </div>
  <div class="signin-business">
    <!-- SIGNIN FORM BUSINESS -->
    <%= render "devise/sessions/shared/form_signin", resource: Business.new, resource_class: Business, resource_name: :business %>
  </div>
</div>


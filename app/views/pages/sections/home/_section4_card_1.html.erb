<div class="container-fluid bg-grey">
  <div id="perks">
    <h2>UNE CARTE GÉNÉREUSE</h2>
    <div class="col-xs-8 col-xs-offset-2 col-sm-2 col-sm-offset-5 border-blue mb-50 mt-15" id="border-blue"></div>
    <div class="row"></div>
    <p class="col-xs-10 col-xs-offset-1 col-sm-10 col-sm-offset-1 text-center">Les commerçants CforGood sont de plus en plus nombreux à offrir des bons plans aux citoyens engagés !</p>
    <p class="col-xs-12 col-sm-10 col-sm-offset-1 text-center mt-10" id="nomobile">Avant de vous les dévoiler tous, découvrez les derniers à nous avoir rejoints :</p>
    <div class="row"></div>
    <div class="container carrousel text-center">
      <% @businesses.each do |business| %>
      <div class="col-xs-12 col-sm-3">
        <div class="perk-card-home">
          <div id="bg-card-home" style="background-image: url(<%= image_url business.picture %>)">
            <div class="box-shadow pt-15">
              <p><%= business.business_category.name%></p>
              <h3><%= business.name%></h3>
              <i class="fa fa-map-marker"> <strong><%=business.city%></strong></i>
              <h4><%= business.perk.upcase%></h4>
            </div>
          </div>
        </div>
      </div>
      <%end%>
    </div>
  </div>
</div>
<div class="container" id="error_explanation">
  <div class="alert alert-danger alert-dismissible" role="alert">
    Le formulaire contient <%= pluralize(current_business.errors.count, "erreur") %>.
    <ul>
      <% current_business.errors.full_messages.each do |msg| %>
          <li class="alert-message"><%= msg %></li>
      <% end %>
    </ul>
  </div>
</div>
